from typing import Any, Dict, List

import pytest


@pytest.fixture
def card_numbers() -> List[str]:
    """
    Фикстура возвращает список номеров карт в разных форматах:
    - без пробелов
    - с пробелами (для проверки форматирования)
    """
    return [
        "1234567812345678",  # Без пробелов
        "1111222233334444",  # Ещё один номер без пробелов
        "5555666677778888",  # Номер для теста генерации
        "1234 5678 1234 5678",  # С пробелами (проверка парсинга)
    ]


@pytest.fixture
def accounts() -> List[str]:
    """
    Фикстура возвращает список номеров счетов:
    - корректные 20-значные номера
    - короткий номер (для проверки обработки ошибок)
    """
    return [
        "12345678901234567890",  # Корректный счёт
        "98765432109876543210",  # Ещё один корректный счёт
        "1234",  # Неправильный номер (для тестов валидации)
    ]


@pytest.fixture
def transactions() -> List[Dict[str, Any]]:
    """
    Фикстура возвращает список транзакций с разными статусами:
    - EXECUTED (успешные)
    - CANCELED (отменённые)
    - PENDING (в ожидании)
    Также содержит даты для тестирования сортировки.
    """
    return [
        {"id": 1, "state": "EXECUTED", "date": "2023-01-15T12:30:00"},
        {"id": 2, "state": "CANCELED", "date": "2023-01-14T11:20:00"},
        {"id": 3, "state": "EXECUTED", "date": "2023-01-16T10:10:00"},
        {"id": 4, "state": "PENDING", "date": "2023-01-13T09:05:00"},
        {"id": 5, "state": "EXECUTED", "date": "2023-01-12T08:00:00"},  # Для теста сортировки
    ]
